### GOPHER SOCIAL 
###
This is an web API that handles the endpoints for the 
Gopher-social application:

Environment variables:
- `ADDR` - server address, default `:8000`
- `JWT_SECRET` - secret used to validate `Bearer` JWT tokens
